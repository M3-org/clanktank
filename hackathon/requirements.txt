# Hackathon Scripts Requirements  – generated 2025-07-19

# ── Core dependencies ─────────────────────────────────────────
python-dotenv==1.1.1
requests==2.32.4

# ── Database ──────────────────────────────────────────────────
sqlalchemy==2.0.41           # uses SQLAlchemy 2.x API

# ── Google Sheets integration ─────────────────────────────────
gspread==6.2.1
google-auth==2.40.3

# ── Discord bot ───────────────────────────────────────────────
discord.py==2.5.2

# ── AI/ML tools (judge scoring) ───────────────────────────────
openai==1.97.0
anthropic==0.58.2

# ── GitHub analysis ───────────────────────────────────────────
PyGithub==2.6.1
gitingest==0.1.5  # required for repository code context in research pipeline
jsonschema==4.23.0  # used for validating GitIngest config responses

# ── Web dashboard (FastAPI backend) ───────────────────────────
fastapi==0.116.1
uvicorn==0.35.0
pydantic==2.11.7
python-multipart>=0.0.22
websockets==12.0  # WebSocket client for Helius real-time updates

# ── YouTube upload / Google APIs ──────────────────────────────
google-api-python-client==2.176.0
google-auth-httplib2==0.2.0
google-auth-oauthlib==1.2.2

# ── Development tools (install with `pip install -r requirements.txt --extra-index-url dev`) ──
pytest==8.4.1
black==25.1.0
flake8==7.3.0

# ── Image processing & logging ────────────────────────────────
Pillow>=12.1.1
structlog==25.4.0
